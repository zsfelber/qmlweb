// TODO : dummy
QmlWeb.registerQmlType({
  module: "QtQuick.Dialogs",
  name: "FileDialog",
  versions: /.*/,
  baseClass: "QtQuick.Item",
  properties: {
  },
  signals: {
  }
}, class {
  constructor(meta) {
    QmlWeb.callSuper(this, meta);

  }

});
